import { Callout } from '../components/Callout'

## 작성 중

또르가 1화에서 "SSE는 응답을 안 닫는 HTTP다"라는 모델을 잡았다. 이 화에서는 클라이언트가 그 흘러오는 바이트를 어떻게 메시지로 조립하는지 — `new EventSource(url)` 한 줄의 정체를 까본다.

<Callout tone="info" title="준비 중">
이 에피소드는 아직 또르가 코드를 짜고 있어요. 곧 만나요.
</Callout>
